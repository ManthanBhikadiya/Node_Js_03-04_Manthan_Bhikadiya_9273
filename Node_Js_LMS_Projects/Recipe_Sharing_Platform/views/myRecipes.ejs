<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe App - My Recipes</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include("navbar") %>

    <div class="container">
        <h1>My Recipes</h1>
        <% if (recipes.length === 0) { %>
            <p class="empty-message">You haven't created any recipes yet.</p>
            <a href="/recipes/add" class="btn">Add your first recipe</a>
        <% } else { %>
            <div class="recipes-grid">
                <% recipes.forEach(r => { %>
                    <div class="card">
                        <% if (r.image) { %>
                            <div class="card-image-container">
                                <img src="<%= r.image %>" alt="<%= r.title %>">
                            </div>
                        <% } %>
                        <div class="card-content">
                            <h2><%= r.title %></h2>
                            <p><strong>Ingredients:</strong> <%= r.ingredients %></p>
                            <div class="card-actions">
                                <a href="/recipes/<%= r._id %>" class="btn btn-sm">View</a>
                                <a href="/recipes/edit/<%= r._id %>" class="btn btn-sm">Edit</a>
                                <form action="/recipes/<%= r._id %>?_method=DELETE" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-sm btn-delete">Delete</button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
</body>
</html>
