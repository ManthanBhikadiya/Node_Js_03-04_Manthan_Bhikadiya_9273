<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <h1>Admin Dashboard</h1>
  <a href="/admin/add" class="btn btn-primary">Add Book</a>

  <div class="grid" style="margin-top:20px;">
    <% books.forEach(book=> { %>
      <div class="card">
        <h3>
          <%= book.title %>
        </h3>
        <p>₹ <%= book.price %>
        </p>
        <a href="/admin/edit/<%= book._id %>" class="btn btn-primary">Edit</a>
        <a href="/admin/delete/<%= book._id %>" class="btn btn-danger">Delete</a>
      </div>
      <% }) %>
  </div>
</body>

</html>